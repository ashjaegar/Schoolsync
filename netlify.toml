[build]
  # The directory where package.json lives
  base = "server"
  
  # The directory to publish (relative to base)
  publish = "client"

# Add any environment variables if needed
[build.environment]
  NODE_VERSION = "20"
